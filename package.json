{
  "name": "inte-team-2026",
  "private": true,
  "description": "Static marketing site — Tailwind CSS build for pages that previously loaded the Tailwind CDN script.",
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind-input.css -o ./tailwind.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
